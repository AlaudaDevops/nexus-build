service:
  nexus:
    enabled: true
    type: ClusterIP

ingress:
  enabled: true
  host: nexus-test-ingress-https-<bdd.instance-id>.example.com
  defaultRule: true
  tls:
    - secretName: "test-tls-cert"
      hosts:
      - nexus-test-ingress-https-<bdd.instance-id>.example.com
